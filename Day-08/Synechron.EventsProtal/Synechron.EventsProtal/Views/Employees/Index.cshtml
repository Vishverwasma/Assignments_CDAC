@using Synechron.EventsProtal.Models
@model List<Employee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@if (Model.Count > 0)
{
    <h1>@ViewBag.PageTitle</h1>
    <hr />
    <h6>@ViewBag.PageSubTitle</h6>
    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>Employee Name</th>
                <th>City</th>
                <th>Email Id</th>
                <th>Contact #</th>
                <th>Show Details</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Employee employee in Model)
            {
                <tr>
                    <td>@employee.Employee_Name</td>
                    <td>@employee.City</td>
                    <td>@employee.Email</td>
                    <td>@employee.Phone_Number</td>
                    <td>
                        @Html.ActionLink("Show Details", "Details", new { id = employee.Employee_ID }, new { @class = "btn btn-info" })
                    </td>
                </tr>
            }
        </tbody>

    </table>
}
else
{
    <h1>Sorry! We didn't find any Employee records'</h1>
}